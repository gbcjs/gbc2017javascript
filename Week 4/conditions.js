'use strict';

function myFunction() {
	var arr = [1, 2, 3, 4, 5];
	arr.push(6);
	for (var i = 0; i <= arr.length - 1; i++) {
		console.log(arr[i] * 2);
	}
	// DECLARATION
	// CONDITION
	// INCREMENT
	// for (declaration, condition, increment) {}

}